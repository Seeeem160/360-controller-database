<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>360 Controller Database</title>

  <meta name="description" content="The definitive Xbox 360 controller database. Identify, authenticate, and explore official, rare, and prototype controllers." />

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --green: #107C10;
      --light: #f8f9fa;
      --card: #ffffff;
      --gray: #e1e1e1;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: var(--light);
      color: #222;
      line-height: 1.6;
    }

    header {
      position: sticky;
      top: 0;
      background: #fff;
      border-bottom: 1px solid var(--gray);
      z-index: 100;
    }

    .nav {
      max-width: 1200px;
      margin: auto;
      padding: 1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-weight: 700;
      font-size: 1.1rem;
      color: var(--green);
    }

    nav a {
      margin-left: 1.2rem;
      text-decoration: none;
      color: #333;
      font-weight: 500;
    }

    nav a:hover {
      color: var(--green);
    }

    .hero {
      max-width: 1200px;
      margin: auto;
      padding: 4rem 1rem;
      text-align: center;
    }

    .hero h1 {
      font-size: 2.6rem;
      margin-bottom: 1rem;
    }

    .hero p {
      max-width: 650px;
      margin: auto;
      margin-bottom: 2rem;
      color: #555;
    }

    .search {
      max-width: 520px;
      margin: auto;
      display: flex;
    }

    .search input {
      flex: 1;
      padding: 0.9rem;
      border-radius: 10px 0 0 10px;
      border: 1px solid var(--gray);
      font-size: 1rem;
    }

    .search button {
      padding: 0.9rem 1.4rem;
      border: none;
      background: var(--green);
      color: #fff;
      border-radius: 0 10px 10px 0;
      cursor: pointer;
      font-weight: 600;
    }

    .section {
      max-width: 1200px;
      margin: auto;
      padding: 2.5rem 1rem;
    }

    .section h2 {
      margin-bottom: 1.5rem;
      font-size: 1.9rem;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 1.5rem;
    }

    .card {
      background: var(--card);
      border-radius: 16px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.06);
      padding: 1.5rem;
      transition: transform 0.2s ease;
      text-decoration: none;
      color: inherit;
      border-left: 6px solid var(--green);
    }

    .card:hover {
      transform: translateY(-4px);
    }

    .card h3 {
      font-size: 1.1rem;
      margin-bottom: 0.4rem;
    }

    .card p {
      font-size: 0.95rem;
      color: #555;
    }

    footer {
      background: #fff;
      border-top: 1px solid var(--gray);
      padding: 2rem 1rem;
      margin-top: 3rem;
    }

    footer p {
      text-align: center;
      font-size: 0.9rem;
      color: #666;
    }

    @media (max-width: 600px) {
      .hero h1 {
        font-size: 2.1rem;
      }
    }
  </style>
</head>

<body>

<header>
  <div class="nav">
    <div class="logo">360 Controller DB</div>
    <nav>
      <a href="index.html">Home</a>
      <a href="controllers.html">Controllers</a>
      <a href="spotting-fakes.html">Spotting Fakes</a>
      <a href="serial-decoder.html">Serial Decoder</a>
      <a href="submit.html">Submit</a>
    </nav>
  </div>
</header>

<section class="hero">
  <h1>Xbox 360 Controller Database</h1>
  <p>
    A complete archive for identifying, authenticating, and collecting
    official Xbox 360 controllers — retail, rare, and prototype.
  </p>

  <div class="search">
    <input type="text" placeholder="Browse the controller database…" disabled />
    <button onclick="location.href='controllers.html'">Browse Controllers</button>
  </div>
</section>

<section class="section">
  <h2>Database Sections</h2>

  <div class="grid">
    <a class="card" href="controllers.html">
      <h3>Retail Controllers</h3>
      <p>Standard and special retail Xbox 360 controllers.</p>
    </a>

    <a class="card" href="controllers.html">
      <h3>Rare & Limited Editions</h3>
      <p>Low-production, regional, and special edition models.</p>
    </a>

    <a class="card" href="controllers.html">
      <h3>Prototypes</h3>
      <p>Development, unreleased, and test controllers.</p>
    </a>
  </div>
</section>

<footer>
  <p>
    Fan-made archive. Not affiliated with Microsoft.<br />
    © 360 Controller Database
  </p>
</footer>

</body>
</html>
